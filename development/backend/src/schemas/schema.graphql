# import * from "models"

type Query {
    getAllListings: [Listing!]!
    getListing(id: ID!): Listing
}

type Mutation {
    createListing(imgURL: String!, title: String!, description: String!, price: Int!): Listing!
}